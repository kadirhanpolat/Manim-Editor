# Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 1 Hızlı Kazanımları uygula: easing mapping düzeltmeleri, WebSocket render takibi, UI test altyapısı, template kütüphanesi, paralel renderer ve render geçmişi.

**Architecture:** 6 bağımsız görev; her biri ayrı ayrı test edilebilir ve commit'lenebilir. WebSocket için API'ye `ws` paketi eklenir, Nginx'e `/ws` proxy location eklenir; mevcut Redis iş kuyruğu yapısı değişmez. Template'ler statik JS objesi olarak tanımlanır, Topbar.vue'nun "New Project" diyaloğuna eklenir.

**Tech Stack:** Node.js 20 + Express + `ws` (WebSocket), Vue 2.7 + Konva.js, Vitest + `@vue/test-utils@1`, Python Manim CE, Redis 7, Docker Compose, Nginx

---

## Kapsam Notu

Bu 6 görev birbirinden bağımsız; herhangi bir sırayla uygulanabilir. Görev 3 (test altyapısı) ilk yapılırsa sonraki görevler testli geliştirilebilir, ama zorunlu değil.

---

## Dosya Haritası

| Dosya | Değişiklik |
|---|---|
| `services/api/src/compiler/codegen.js` | EASING_MAP düzelt (7 yanlış/eksik giriş) |
| `services/web/src/export/manim.js` | EASING_MAP düzelt (aynı değişiklik — iki kopya senkron tutulur) |
| `services/web/tests/engine.test.mjs` | Easing mapping test senaryoları ekle |
| `services/api/src/ws.js` | Yeni — WebSocket sunucusu + Redis polling |
| `services/api/src/index.js` | `http.createServer` + `attachWebSocket` |
| `services/api/package.json` | `ws` bağımlılığı ekle |
| `services/web/nginx.conf` | `/ws` proxy location ekle |
| `services/web/src/api.js` | `connectJobWebSocket` fonksiyonu ekle |
| `services/web/src/store/project.js` | `_startPollRender` → WebSocket ile değiştir |
| `services/web/package.json` | `vitest`, `@vue/test-utils@1`, `jsdom` ekle |
| `services/web/vitest.config.js` | Yeni — Vitest yapılandırması |
| `services/web/tests/components/store.test.js` | Yeni — store action testleri |
| `services/web/src/templates/index.js` | Yeni — 5 hazır proje şablonu |
| `services/web/src/components/topbar/Topbar.vue` | Şablon seçici adımı ekle |
| `docker-compose.yml` | `renderer-2` servisi ekle |
| `services/renderer/worker.py` | Numaralı render dosyası kaydetme |
| `services/api/src/routes/renders.js` | Render listesi endpoint'i güncelle |
| `services/web/src/api.js` | `renders.list` fonksiyonu ekle |
| `services/web/src/App.vue` | Render diyaloğuna geçmiş listesi ekle |

---

## Görev 1: Easing Mapping Düzeltmeleri

**Dosyalar:**
- Değiştir: `services/api/src/compiler/codegen.js:13-20`
- Değiştir: `services/web/src/export/manim.js:13-20`
- Değiştir: `services/web/tests/engine.test.mjs`

**Sorun:** `EASING_MAP` hem `codegen.js` (sunucu tarafı) hem `manim.js` (istemci tarafı) içinde tanımlı. İkisinde de aynı hatalar var: `ease_in_out_cubic` ve `spring` `smooth`'a eşlenmiş, 7 easing türü eksik.

- [ ] **Adım 1.1: Test — mevcut hatalı eşlemeleri belgele**

`services/web/tests/engine.test.mjs` dosyasının sonuna ekle:

```js
// ── Easing map tests ──────────────────────────────────────────────────────────
import { EASING_MAP } from '../src/export/manim.js';

function assert(cond, msg) { if (!cond) throw new Error('FAIL: ' + msg); }

// These should NOT map to 'rate_functions.smooth' (they have dedicated Manim functions)
assert(EASING_MAP.ease_in_out_cubic !== 'rate_functions.smooth', 'ease_in_out_cubic must not be smooth');
assert(EASING_MAP.spring !== 'rate_functions.smooth', 'spring must not be smooth');

// These must exist (were missing entirely)
assert(EASING_MAP.ease_in_quart, 'ease_in_quart must be mapped');
assert(EASING_MAP.ease_out_quart, 'ease_out_quart must be mapped');
assert(EASING_MAP.ease_in_out_quart, 'ease_in_out_quart must be mapped');
assert(EASING_MAP.ease_in_out_back, 'ease_in_out_back must be mapped');
assert(EASING_MAP.ease_out_elastic, 'ease_out_elastic must be mapped');
assert(EASING_MAP.ease_in_elastic, 'ease_in_elastic must be mapped');

// Total: all 17 entries must be present
const EXPECTED_KEYS = [
  'linear','ease_in','ease_out','ease_in_out',
  'ease_in_cubic','ease_out_cubic','ease_in_out_cubic',
  'ease_in_quart','ease_out_quart','ease_in_out_quart',
  'ease_in_back','ease_out_back','ease_in_out_back',
  'ease_out_elastic','ease_in_elastic','ease_out_bounce','spring'
];
for (const k of EXPECTED_KEYS) assert(EASING_MAP[k], `EASING_MAP missing key: ${k}`);

console.log('✓ EASING_MAP: all 17 keys present and correctly mapped');
```

- [ ] **Adım 1.2: Test çalıştır — FAIL beklenir**

```bash
cd services/web && node tests/engine.test.mjs
```

Beklenen: `FAIL: ease_in_out_cubic must not be smooth`

- [ ] **Adım 1.3: `services/web/src/export/manim.js` EASING_MAP'i düzelt (satır 13-20)**

```js
const EASING_MAP = {
  linear:            'linear',
  ease_in:           'rate_functions.ease_in_sine',
  ease_out:          'rate_functions.ease_out_sine',
  ease_in_out:       'rate_functions.smooth',
  ease_in_cubic:     'rate_functions.ease_in_cubic',
  ease_out_cubic:    'rate_functions.ease_out_cubic',
  ease_in_out_cubic: 'rate_functions.ease_in_out_cubic',
  ease_in_quart:     'rate_functions.ease_in_quart',
  ease_out_quart:    'rate_functions.ease_out_quart',
  ease_in_out_quart: 'rate_functions.ease_in_out_quart',
  ease_in_back:      'rate_functions.ease_in_back',
  ease_out_back:     'rate_functions.ease_out_back',
  ease_in_out_back:  'rate_functions.ease_in_out_back',
  ease_out_elastic:  'rate_functions.ease_out_elastic',
  ease_in_elastic:   'rate_functions.ease_in_elastic',
  ease_out_bounce:   'rate_functions.ease_out_bounce',
  spring:            'rate_functions.ease_out_elastic',
};
```

- [ ] **Adım 1.4: Aynı değişikliği `services/api/src/compiler/codegen.js` satır 13-20'ye uygula**

```js
const EASING_MAP = {
  linear:            'linear',
  ease_in:           'rate_functions.ease_in_sine',
  ease_out:          'rate_functions.ease_out_sine',
  ease_in_out:       'rate_functions.smooth',
  ease_in_cubic:     'rate_functions.ease_in_cubic',
  ease_out_cubic:    'rate_functions.ease_out_cubic',
  ease_in_out_cubic: 'rate_functions.ease_in_out_cubic',
  ease_in_quart:     'rate_functions.ease_in_quart',
  ease_out_quart:    'rate_functions.ease_out_quart',
  ease_in_out_quart: 'rate_functions.ease_in_out_quart',
  ease_in_back:      'rate_functions.ease_in_back',
  ease_out_back:     'rate_functions.ease_out_back',
  ease_in_out_back:  'rate_functions.ease_in_out_back',
  ease_out_elastic:  'rate_functions.ease_out_elastic',
  ease_in_elastic:   'rate_functions.ease_in_elastic',
  ease_out_bounce:   'rate_functions.ease_out_bounce',
  spring:            'rate_functions.ease_out_elastic',
};
```

- [ ] **Adım 1.5: Test çalıştır — PASS beklenir**

```bash
cd services/web && node tests/engine.test.mjs
```

Beklenen: `✓ EASING_MAP: all 17 keys present and correctly mapped`

- [ ] **Adım 1.6: Commit**

```bash
git add services/api/src/compiler/codegen.js services/web/src/export/manim.js services/web/tests/engine.test.mjs
git commit -m "fix: correct easing map — 7 missing/wrong entries now point to proper Manim rate_functions"
```

---

## Görev 2: WebSocket Render Takibi

**Dosyalar:**
- Oluştur: `services/api/src/ws.js`
- Değiştir: `services/api/src/index.js`
- Değiştir: `services/api/package.json`
- Değiştir: `services/web/nginx.conf`
- Değiştir: `services/web/src/api.js`
- Değiştir: `services/web/src/store/project.js`

**Yaklaşım:** API tarafında sunucu-taraflı Redis polling (500ms), WebSocket push ile frontend'e iletilir. İstemci artık `setInterval` yapmaz; `ws://host/ws` üzerine bağlanır ve `{ type: 'subscribe', jobId }` mesajı gönderir. `worker.py` değişmez.

- [ ] **Adım 2.1: `services/api/package.json`'a `ws` ekle**

`"dependencies"` bloğuna `"ws": "^8.18.0"` satırını ekle:

```json
{
  "name": "manim-motion-api",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "multer": "^1.4.5-lts.1",
    "redis": "^4.6.12",
    "uuid": "^9.0.1",
    "ws": "^8.18.0",
    "zod": "^3.22.4"
  }
}
```

- [ ] **Adım 2.2: `services/api/src/ws.js` oluştur**

```js
import { WebSocketServer } from 'ws';
import { getJobStatus } from './queue.js';

const DONE_STATUSES = new Set(['completed', 'failed']);
const subscriptions = new Map(); // jobId -> Set<WebSocket>

export function attachWebSocket(server) {
  const wss = new WebSocketServer({ server, path: '/ws' });

  wss.on('connection', (ws) => {
    ws.on('message', (data) => {
      let msg;
      try { msg = JSON.parse(data.toString()); } catch { return; }
      if (msg.type === 'subscribe' && msg.jobId) {
        if (!subscriptions.has(msg.jobId)) subscriptions.set(msg.jobId, new Set());
        subscriptions.get(msg.jobId).add(ws);
        pollUntilDone(msg.jobId);
      }
    });

    ws.on('close', () => {
      for (const [jobId, clients] of subscriptions.entries()) {
        clients.delete(ws);
        if (clients.size === 0) subscriptions.delete(jobId);
      }
    });
  });
}

function broadcast(jobId, payload) {
  const clients = subscriptions.get(jobId);
  if (!clients) return;
  const msg = JSON.stringify(payload);
  for (const ws of clients) {
    if (ws.readyState === 1) ws.send(msg);
  }
}

async function pollUntilDone(jobId) {
  while (subscriptions.has(jobId)) {
    await new Promise((r) => setTimeout(r, 500));
    const job = await getJobStatus(jobId).catch(() => null);
    if (!job) continue;
    broadcast(jobId, {
      type: 'job_update',
      jobId,
      status: job.status,
      stdout: job.stdout || '',
      stderr: job.stderr || '',
      error:  job.error  || '',
    });
    if (DONE_STATUSES.has(job.status)) {
      subscriptions.delete(jobId);
      break;
    }
  }
}
```

- [ ] **Adım 2.3: `services/api/src/index.js` — `http.createServer` + WebSocket bağla**

Tüm dosyayı şu içerikle değiştir:

```js
import express from 'express';
import http from 'http';
import cors from 'cors';
import helmet from 'helmet';
import path from 'path';
import { fileURLToPath } from 'url';

import projectsRouter from './routes/projects.js';
import assetsRouter   from './routes/assets.js';
import rendersRouter  from './routes/renders.js';
import jobsRouter     from './routes/jobs.js';
import fontsRouter    from './routes/fonts.js';
import { attachWebSocket } from './ws.js';

const __dirname = path.dirname(fileURLToPath(import.meta.url));

const app    = express();
const server = http.createServer(app);
const PORT   = process.env.PORT || 3000;
const DATA_DIR = process.env.DATA_DIR || '/data';

app.use(helmet());
app.use(cors());
app.use(express.json({ limit: '50mb' }));

app.use((req, res, next) => { req.dataDir = DATA_DIR; next(); });

app.get('/health', (req, res) => res.json({ status: 'ok', timestamp: new Date().toISOString() }));

app.use('/api/projects', projectsRouter);
app.use('/api/assets',   assetsRouter);
app.use('/api/renders',  rendersRouter);
app.use('/api/jobs',     jobsRouter);
app.use('/api/fonts',    fontsRouter);

app.use((err, req, res, _next) => {
  console.error('[API Error]', err);
  res.status(err.status || 500).json({ error: err.message || 'Internal server error' });
});

attachWebSocket(server);

server.listen(PORT, () => {
  console.log(`[API] Server running on port ${PORT}`);
  console.log(`[API] Data directory: ${DATA_DIR}`);
});
```

- [ ] **Adım 2.4: `services/web/nginx.conf` — `/ws` proxy location ekle**

`location ^~ /api/` bloğundan önce şu bloğu ekle:

```nginx
    # WebSocket proxy for render job updates
    location /ws {
        proxy_pass http://api:3000/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 300s;
    }
```

- [ ] **Adım 2.5: `services/web/src/api.js` — `connectJobWebSocket` ekle**

Dosyanın sonundaki `export default` satırından önce şu bloğu ekle:

```js
// ─── WebSocket ────────────────────────────────────────────────────────────────

/**
 * Connect to the job WebSocket and subscribe to updates for a specific job.
 * @param {string} jobId
 * @param {function({status, stdout, stderr, error}): void} onUpdate
 * @returns {function(): void} disconnect function
 */
export function connectJobWebSocket(jobId, onUpdate) {
  const protocol = location.protocol === 'https:' ? 'wss:' : 'ws:';
  const ws = new WebSocket(`${protocol}//${location.host}/ws`);

  ws.onopen = () => ws.send(JSON.stringify({ type: 'subscribe', jobId }));
  ws.onmessage = (event) => {
    try {
      const msg = JSON.parse(event.data);
      if (msg.type === 'job_update' && msg.jobId === jobId) onUpdate(msg);
    } catch {}
  };
  ws.onerror = () => ws.close();

  return () => ws.close();
}
```

Ayrıca `export default` satırını güncelle:

```js
export default { projects, assets, jobs, renders, checkHealth, connectJobWebSocket };
```

- [ ] **Adım 2.6: `services/web/src/store/project.js` — polling'i WebSocket ile değiştir**

Dosyanın başına `connectJobWebSocket` import'unu ekle (mevcut `import api` satırını bul, altına ekle):

```js
import { connectJobWebSocket } from '../api.js';
```

`_startPollRender` metodunu (satır ~901-925) şununla değiştir:

```js
  _startPollRender(jobId, projectId) {
    actions._stopPollRender();

    _pollDisconnect = connectJobWebSocket(jobId, (msg) => {
      if (msg.status === 'running') {
        store.renderStatus = 'running';
        if (msg.stdout) store.renderLog = msg.stdout;
      } else if (msg.status === 'completed') {
        store.renderStatus = 'completed';
        store.renderVideoUrl = api.renders.getLatestUrl(projectId);
        store.renderLog = msg.stdout || '';
        actions._stopPollRender();
      } else if (msg.status === 'failed') {
        store.renderStatus = 'failed';
        store.renderError = msg.error || msg.stderr || 'Render failed';
        store.renderLog = (msg.stdout || '') + '\n' + (msg.stderr || '');
        actions._stopPollRender();
      }
    });
  },
```

`_stopPollRender` metodunu şununla değiştir:

```js
  _stopPollRender() {
    if (_pollDisconnect) {
      _pollDisconnect();
      _pollDisconnect = null;
    }
  },
```

Dosyanın üstündeki `let _pollTimer = null;` satırını şununla değiştir:

```js
let _pollDisconnect = null;
```

- [ ] **Adım 2.7: API container'ında `ws` kur**

```bash
cd services/api && npm install
```

- [ ] **Adım 2.8: Docker ile test et**

```bash
docker compose up --build
```

Tarayıcıda `http://localhost:8080` aç, bir render başlat. DevTools → Network → WS sekmesinde `/ws` bağlantısı görünmeli ve `job_update` mesajları gelmeli.

- [ ] **Adım 2.9: Commit**

```bash
git add services/api/src/ws.js services/api/src/index.js services/api/package.json \
        services/web/nginx.conf services/web/src/api.js services/web/src/store/project.js
git commit -m "feat: replace render polling with WebSocket push — server-side 500ms Redis poll, client subscribes by jobId"
```

---

## Görev 3: UI Bileşen Test Altyapısı

**Dosyalar:**
- Değiştir: `services/web/package.json`
- Oluştur: `services/web/vitest.config.js`
- Oluştur: `services/web/tests/components/store.test.js`

**Not:** Vue 2 ile uyumlu test utils sürümü `@vue/test-utils@1.x`'tir (`@2` sürümü Vue 3 içindir).

- [ ] **Adım 3.1: `services/web/package.json`'a test bağımlılıkları ekle**

`"devDependencies"` bloğunu şununla değiştir:

```json
"devDependencies": {
  "@vitejs/plugin-vue2": "^2.3.1",
  "@vue/test-utils": "^1.3.6",
  "autoprefixer": "^10.4.17",
  "jsdom": "^26.0.0",
  "postcss": "^8.4.35",
  "tailwindcss": "^3.4.1",
  "vite": "^5.0.12",
  "vitest": "^2.0.0"
}
```

`"scripts"` bloğunu şununla değiştir:

```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "test": "node tests/engine.test.mjs",
  "test:unit": "vitest run",
  "test:unit:watch": "vitest"
}
```

- [ ] **Adım 3.2: `services/web/vitest.config.js` oluştur**

```js
import { defineConfig } from 'vitest/config';
import vue from '@vitejs/plugin-vue2';

export default defineConfig({
  plugins: [vue()],
  test: {
    environment: 'jsdom',
    globals: true,
  },
});
```

- [ ] **Adım 3.3: Bağımlılıkları kur**

```bash
cd services/web && npm install
```

- [ ] **Adım 3.4: İlk bileşen testini yaz — store actions**

`services/web/tests/components/store.test.js` oluştur:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { store, actions } from '../../src/store/project.js';

beforeEach(() => {
  actions.newProject('Test', 'visual');
});

describe('addObject', () => {
  it('adds a circle to the project', () => {
    const obj = actions.addObject('circle', 960, 540);
    expect(obj.type).toBe('circle');
    expect(store.project.objects).toHaveLength(1);
    expect(store.project.objects[0].id).toBe(obj.id);
  });

  it('sets sensible defaults', () => {
    const obj = actions.addObject('rectangle', 100, 100);
    expect(obj.width).toBeGreaterThan(0);
    expect(obj.height).toBeGreaterThan(0);
    expect(obj.opacity).toBe(1);
    expect(obj.enterAnim).toBe('fade_in');
  });
});

describe('deleteObject', () => {
  it('removes object and its clips', () => {
    const obj = actions.addObject('circle', 960, 540);
    actions.addClip(0, { type: 'fade', sourceId: obj.id, startTime: 0, duration: 1 });
    actions.deleteObject(obj.id);
    expect(store.project.objects).toHaveLength(0);
    expect(store.project.tracks[0].clips).toHaveLength(0);
  });
});

describe('undo/redo', () => {
  it('reverts addObject', () => {
    actions.addObject('circle', 960, 540);
    expect(store.project.objects).toHaveLength(1);
    actions.undo();
    expect(store.project.objects).toHaveLength(0);
  });

  it('redo reapplies after undo', () => {
    actions.addObject('circle', 960, 540);
    actions.undo();
    actions.redo();
    expect(store.project.objects).toHaveLength(1);
  });
});

describe('groupObjects', () => {
  it('groups two objects', () => {
    const a = actions.addObject('circle', 400, 400);
    const b = actions.addObject('square', 600, 400);
    const group = actions.groupObjects([a.id, b.id]);
    expect(group).not.toBeNull();
    expect(store.project.groups).toHaveLength(1);
    expect(store.project.groups[0].childIds).toContain(a.id);
  });

  it('returns null for fewer than 2 objects', () => {
    const a = actions.addObject('circle', 400, 400);
    const result = actions.groupObjects([a.id]);
    expect(result).toBeNull();
  });
});
```

- [ ] **Adım 3.5: Testleri çalıştır — PASS beklenir**

```bash
cd services/web && npm run test:unit
```

Beklenen: Tüm testler yeşil.

- [ ] **Adım 3.6: Commit**

```bash
git add services/web/package.json services/web/vitest.config.js services/web/tests/components/store.test.js
git commit -m "feat: add Vitest unit test infrastructure + store action tests"
```

---

## Görev 4: Template / Preset Kütüphanesi

**Dosyalar:**
- Oluştur: `services/web/src/templates/index.js`
- Değiştir: `services/web/src/components/topbar/Topbar.vue`

**Yaklaşım:** 5 hazır proje JSON'u statik olarak tanımla. "New Project" diyaloğuna üçüncü bir adım ekle: "Boş Proje" vs. "Şablon Seç". Şablon seçilirse `actions.importJSON` ile direkt yüklenir.

- [ ] **Adım 4.1: `services/web/src/templates/index.js` oluştur**

```js
function uid(prefix) {
  return `${prefix}_tpl_${Math.random().toString(36).slice(2, 9)}`;
}

const STAGE = { width: 1920, height: 1080, backgroundColor: '#0f0f0f', backgroundOpacity: 1, backgroundImage: null, gridVisible: true, gridSize: 8, gridColor: '#ffffff', gridOpacity: 0.12, snapEnabled: true, snapToGrid: true, snapToCenter: true, snapToObjects: false };

export const TEMPLATES = [
  {
    id: 'blank',
    label: 'Boş Proje',
    description: 'Sıfırdan başla',
    icon: '□',
    project: null, // null → normal newProject akışı
  },
  {
    id: 'formula_reveal',
    label: 'Formül Tanıtım',
    description: 'LaTeX formülü yazma efektiyle ortaya çıkar',
    icon: '∑',
    project: () => {
      const id1 = uid('obj');
      return {
        name: 'Formül Tanıtım',
        editorMode: 'visual',
        codeSource: '',
        stage: { ...STAGE },
        assets: [],
        groups: [],
        sceneDuration: 6,
        objects: [
          {
            id: id1, type: 'latex', name: 'Formül',
            x: 960, y: 540, width: 300, height: 100, rotation: 0,
            fill: '#ffffff', stroke: 'transparent', strokeWidth: 0, opacity: 1, zOrder: 0,
            latex: 'E = mc^2',
            enterTime: 0, duration: 5, enterAnim: 'write', exitAnim: 'none',
            enterAnimDur: 1.5, exitAnimDur: 0.5,
          },
        ],
        tracks: [
          { id: 'track_1', name: 'Track 1', clips: [] },
        ],
      };
    },
  },
  {
    id: 'shape_transform',
    label: 'Şekil Dönüşümü',
    description: 'Bir şekil diğerine morph olur',
    icon: '⇌',
    project: () => {
      const src = uid('obj');
      const tgt = uid('obj');
      const clip = uid('clip');
      return {
        name: 'Şekil Dönüşümü',
        editorMode: 'visual',
        codeSource: '',
        stage: { ...STAGE },
        assets: [],
        groups: [],
        sceneDuration: 6,
        objects: [
          {
            id: src, type: 'circle', name: 'Kaynak',
            x: 960, y: 540, width: 200, height: 200, rotation: 0,
            fill: '#3b82f6', stroke: '#ffffff', strokeWidth: 2, opacity: 1, zOrder: 0,
            enterTime: 0, duration: 4, enterAnim: 'grow_in', exitAnim: 'none',
            enterAnimDur: 0.5, exitAnimDur: 0.5,
          },
          {
            id: tgt, type: 'square', name: 'Hedef',
            x: 960, y: 540, width: 200, height: 200, rotation: 0,
            fill: '#22c55e', stroke: '#ffffff', strokeWidth: 2, opacity: 1, zOrder: 1,
            enterTime: 0, duration: 4, enterAnim: 'none', exitAnim: 'none',
            enterAnimDur: 0.5, exitAnimDur: 0.5,
          },
        ],
        tracks: [
          {
            id: 'track_1', name: 'Track 1',
            clips: [
              {
                id: clip, type: 'transform', startTime: 1.5, duration: 1.5,
                easing: 'ease_in_out_cubic', sourceId: src, targetId: tgt,
                params: {}, overshoot: 0, settle: 1.0, morphQuality: 'medium',
              },
            ],
          },
          { id: 'track_2', name: 'Track 2', clips: [] },
        ],
      };
    },
  },
  {
    id: 'title_slide',
    label: 'Başlık Slaydı',
    description: 'Başlık ve alt başlık kademeli giriş',
    icon: 'T',
    project: () => {
      const title = uid('obj');
      const sub   = uid('obj');
      return {
        name: 'Başlık Slaydı',
        editorMode: 'visual',
        codeSource: '',
        stage: { ...STAGE },
        assets: [],
        groups: [],
        sceneDuration: 8,
        objects: [
          {
            id: title, type: 'text', name: 'Başlık',
            x: 960, y: 480, width: 800, height: 80, rotation: 0,
            fill: '#ffffff', stroke: 'transparent', strokeWidth: 0, opacity: 1, zOrder: 0,
            content: 'Başlık Metni', fontSize: 72, fontFamily: 'Roboto',
            textAlign: 'center', fontWeight: 'bold', fontStyle: 'normal',
            enterTime: 0, duration: 7, enterAnim: 'fly_in_bottom', exitAnim: 'none',
            enterAnimDur: 0.6, exitAnimDur: 0.5,
          },
          {
            id: sub, type: 'text', name: 'Alt Başlık',
            x: 960, y: 600, width: 600, height: 50, rotation: 0,
            fill: '#94a3b8', stroke: 'transparent', strokeWidth: 0, opacity: 1, zOrder: 1,
            content: 'Alt başlık açıklaması', fontSize: 36, fontFamily: 'Roboto',
            textAlign: 'center', fontWeight: 'normal', fontStyle: 'normal',
            enterTime: 0.5, duration: 6.5, enterAnim: 'fade_in', exitAnim: 'none',
            enterAnimDur: 0.8, exitAnimDur: 0.5,
          },
        ],
        tracks: [
          { id: 'track_1', name: 'Track 1', clips: [] },
        ],
      };
    },
  },
  {
    id: 'axes_intro',
    label: 'Koordinat Sistemi',
    description: 'Eksenler sahneye çizilir',
    icon: '⊕',
    project: () => {
      const ax = uid('obj');
      const lbl = uid('obj');
      return {
        name: 'Koordinat Sistemi',
        editorMode: 'visual',
        codeSource: '',
        stage: { ...STAGE },
        assets: [],
        groups: [],
        sceneDuration: 6,
        objects: [
          {
            id: ax, type: 'axes', name: 'Eksenler',
            x: 960, y: 540, width: 800, height: 500, rotation: 0,
            fill: '#ffffff', stroke: '#ffffff', strokeWidth: 2, opacity: 1, zOrder: 0,
            xRange: [-5, 5, 1], yRange: [-3, 3, 1],
            enterTime: 0, duration: 5, enterAnim: 'draw', exitAnim: 'none',
            enterAnimDur: 1.2, exitAnimDur: 0.5,
          },
          {
            id: lbl, type: 'text', name: 'Etiket',
            x: 960, y: 200, width: 500, height: 50, rotation: 0,
            fill: '#94a3b8', stroke: 'transparent', strokeWidth: 0, opacity: 1, zOrder: 1,
            content: 'f(x) = x²', fontSize: 32, fontFamily: 'Roboto',
            textAlign: 'center', fontWeight: 'normal', fontStyle: 'normal',
            enterTime: 1.5, duration: 3.5, enterAnim: 'fade_in', exitAnim: 'none',
            enterAnimDur: 0.5, exitAnimDur: 0.5,
          },
        ],
        tracks: [
          { id: 'track_1', name: 'Track 1', clips: [] },
        ],
      };
    },
  },
];

export default TEMPLATES;
```

- [ ] **Adım 4.2: Template veri testini yaz**

`services/web/tests/components/store.test.js` sonuna ekle:

```js
import TEMPLATES from '../../src/templates/index.js';

describe('TEMPLATES', () => {
  it('has 5 entries', () => {
    expect(TEMPLATES).toHaveLength(5);
  });

  it('each non-blank template returns a valid project', () => {
    for (const tpl of TEMPLATES.filter(t => t.project !== null)) {
      const p = tpl.project();
      expect(p.objects.length).toBeGreaterThan(0);
      expect(p.stage).toBeDefined();
      expect(Array.isArray(p.tracks)).toBe(true);
    }
  });
});
```

- [ ] **Adım 4.3: Test çalıştır — PASS beklenir**

```bash
cd services/web && npm run test:unit
```

- [ ] **Adım 4.4: `Topbar.vue` `np-dialog` içinde şablon seçiciyi ekle**

`Topbar.vue` içindeki `np-dialog` bölümünü bul (satır ~188–231). `data()` metoduna `newProjectTemplate: null` ekle. `confirmNewProject` metodunu güncelle.

**`data()` içindeki mevcut alanlar** (`showNewProjectDialog`, `newProjectName`, `newProjectMode`) yanına ekle:

```js
newProjectTemplate: null,
```

**`Topbar.vue` script bölümüne import ekle** (`import { store, actions }` satırının altına):

```js
import TEMPLATES from '../../templates/index.js';
```

**`computed` bölümüne ekle:**

```js
templates() { return TEMPLATES; },
```

**`confirmNewProject` metodunu güncelle:**

```js
confirmNewProject() {
  const name = this.newProjectName.trim() || 'My Animation';
  const tpl  = this.newProjectTemplate;

  if (tpl && tpl.project) {
    const projectData = tpl.project();
    projectData.name = name;
    projectData.id   = null;
    actions.importJSON(JSON.stringify(projectData));
  } else {
    actions.newProject(name, this.newProjectMode);
  }
  this.showNewProjectDialog = false;
  this.newProjectName       = 'My Animation';
  this.newProjectTemplate   = null;
},
```

**`cancelNewProject` metodunu güncelle:**

```js
cancelNewProject() {
  this.showNewProjectDialog = false;
  this.newProjectName       = 'My Animation';
  this.newProjectTemplate   = null;
},
```

**`np-dialog` şablonundaki `np-mode-row` bölümünden sonra** şablon seçici ekle (Sadece `newProjectMode === 'visual'` iken göster):

```html
<!-- Template selector (only for visual mode) -->
<template v-if="newProjectMode === 'visual'">
  <label class="np-label" style="margin-top:12px;">Şablon</label>
  <div style="display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-top:4px;">
    <button
      v-for="tpl in templates"
      :key="tpl.id"
      class="np-mode-btn"
      :class="{ active: newProjectTemplate && newProjectTemplate.id === tpl.id || (!newProjectTemplate && tpl.id === 'blank') }"
      @click="newProjectTemplate = tpl.id === 'blank' ? null : tpl"
      style="text-align:left; padding:10px 12px;"
    >
      <span style="font-size:18px; display:block; margin-bottom:4px;">{{ tpl.icon }}</span>
      <span class="np-mode-label">{{ tpl.label }}</span>
      <span class="np-mode-desc">{{ tpl.description }}</span>
    </button>
  </div>
</template>
```

- [ ] **Adım 4.5: Manuel test — tarayıcıda doğrula**

```bash
cd services/web && npm run dev
```

`http://localhost:5173` aç → File → New Project → Visual seç → Şablon listesi görünmeli → "Formül Tanıtım" seç → Create → Canvas'ta LaTeX nesnesi görünmeli.

- [ ] **Adım 4.6: Commit**

```bash
git add services/web/src/templates/index.js services/web/src/components/topbar/Topbar.vue services/web/tests/components/store.test.js
git commit -m "feat: add 5 project templates (formula reveal, shape transform, title slide, axes, blank)"
```

---

## Görev 5: Paralel Render Worker

**Dosyalar:**
- Değiştir: `docker-compose.yml`

Redis kuyruğu (`render:queue`) FIFO'dur; iki worker aynı kuyruktan çeker, çakışma yoktur.

- [x] **Adım 5.1: `docker-compose.yml`'de `renderer` servisinden sonra `renderer-2` ekle**

`renderer:` bloğundan sonra şunu ekle:

```yaml
  renderer-2:
    build: ./services/renderer
    environment:
      - DATA_DIR=/data
      - REDIS_URL=redis://redis:6379
    volumes:
      - manim_motion_data:/data
    depends_on:
      init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python3", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 5
```

- [x] **Adım 5.2: Test — iki worker başlatıldığını doğrula**

```bash
docker compose up --build -d
docker compose ps
```

Beklenen çıktı: `renderer` ve `renderer-2` her ikisi de `running` durumunda.

- [x] **Adım 5.3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: add second renderer worker for parallel job processing"
```

---

## Görev 6: Render Geçmişi

**Dosyalar:**
- Değiştir: `services/renderer/worker.py`
- Değiştir: `services/api/src/routes/renders.js`
- Değiştir: `services/web/src/api.js`
- Değiştir: `services/web/src/App.vue`

**Yaklaşım:** Worker her render sonunda tarihli kopya kaydeder (`render_YYYYMMDD_HHMMSS.mp4`). API bu dosyaları listeler. Frontend render diyaloğunun altında en son 5 render gösterilir.

- [ ] **Adım 6.1: `services/renderer/worker.py` — numaralı kopya kaydet**

`render_job` fonksiyonunun içinde, `latest_link` kopyası yapıldıktan sonra (satır ~118 civarı) şunu ekle:

```python
# Save a timestamped copy for history (keep last 5)
if output_video and os.path.exists(output_video):
    timestamp = time.strftime("%Y%m%d_%H%M%S", time.gmtime())
    history_path = os.path.join(media_dir, f"render_{timestamp}.mp4")
    shutil.copy2(output_video, history_path)

    # Prune: keep only the 5 most recent history files
    history_files = sorted(
        [f for f in os.listdir(media_dir) if f.startswith("render_") and f.endswith(".mp4")],
        reverse=True
    )
    for old in history_files[5:]:
        try:
            os.remove(os.path.join(media_dir, old))
        except Exception:
            pass
```

- [ ] **Adım 6.2: `services/api/src/routes/renders.js` — geçmiş endpoint güncelle**

Mevcut `GET /:projectId` route'unu (satır ~84–117) şununla değiştir:

```js
/**
 * List all renders for a project.
 * GET /api/renders/:projectId
 */
router.get('/:projectId', async (req, res, next) => {
  try {
    const rendersDir = path.join(req.dataDir, 'renders', req.params.projectId);

    try { await fs.access(rendersDir); } catch {
      return res.json({ renders: [], hasLatest: false });
    }

    const latestPath = path.join(rendersDir, 'latest.mp4');
    let hasLatest = false;
    let latestStats = null;
    try { latestStats = await fs.stat(latestPath); hasLatest = true; } catch {}

    // Collect timestamped history files
    const entries = await fs.readdir(rendersDir);
    const historyFiles = entries
      .filter(f => f.startsWith('render_') && f.endsWith('.mp4'))
      .sort()
      .reverse()
      .slice(0, 5);

    const history = await Promise.all(
      historyFiles.map(async (name) => {
        const fPath = path.join(rendersDir, name);
        const stat  = await fs.stat(fPath).catch(() => null);
        return stat ? { name, size: stat.size, modifiedAt: stat.mtime, url: `/api/renders/${req.params.projectId}/${name}` } : null;
      })
    );

    res.json({
      renders: hasLatest ? [{ name: 'latest.mp4', size: latestStats.size, modifiedAt: latestStats.mtime, url: `/api/renders/${req.params.projectId}/latest.mp4` }] : [],
      hasLatest,
      history: history.filter(Boolean),
    });
  } catch (err) {
    next(err);
  }
});
```

Ayrıca geçmiş dosyasını servis edecek bir route ekle — mevcut `GET /:projectId/latest.mp4` route'undan sonra:

```js
/**
 * Serve a specific history render file.
 * GET /api/renders/:projectId/:filename
 */
router.get('/:projectId/:filename', async (req, res, next) => {
  try {
    const { projectId, filename } = req.params;
    // Only allow safe filenames
    if (!/^[\w.-]+\.mp4$/.test(filename)) return res.status(400).json({ error: 'Invalid filename' });

    const filePath = path.join(req.dataDir, 'renders', projectId, filename);
    await fs.access(filePath);
    res.sendFile(filePath);
  } catch (err) {
    if (err.code === 'ENOENT') return res.status(404).json({ error: 'File not found' });
    next(err);
  }
});
```

- [ ] **Adım 6.3: `services/web/src/api.js` — `renders.list` ekle**

`renders` nesnesine `list` fonksiyonu ekle:

```js
export const renders = {
  getLatestUrl: (projectId) => `${API_BASE}/renders/${projectId}/latest.mp4?t=${Date.now()}`,
  getInfo:      (projectId) => request(`/renders/${projectId}`),
  list:         (projectId) => request(`/renders/${projectId}`),
};
```

- [ ] **Adım 6.4: `services/web/src/App.vue` — render geçmişi göster**

`App.vue`'daki "Render Again" butonunun (`resetRender`) hemen altına render geçmişi bölümü ekle:

```html
<!-- Render History -->
<div v-if="renderHistory.length > 0" class="mt-4">
  <p class="text-xs font-medium uppercase tracking-wider mb-2" style="color: var(--studio-text-muted);">Render Geçmişi</p>
  <div class="space-y-1.5">
    <div v-for="r in renderHistory" :key="r.name" class="flex items-center justify-between px-3 py-2 rounded-lg" style="background: var(--studio-bg);">
      <span class="text-[11px] font-mono" style="color: var(--studio-text-muted);">{{ formatRenderDate(r.name) }}</span>
      <a :href="r.url" download class="text-[11px] px-2 py-1 rounded" style="color: var(--studio-accent); background: var(--studio-accent-subtle);">İndir</a>
    </div>
  </div>
</div>
```

`App.vue` script bölümünde `data()` içine ekle:

```js
renderHistory: [],
```

`watch` bölümüne ekle:

```js
renderStatus(status) {
  if (status === 'completed' && this.projectId) {
    this.loadRenderHistory();
  }
}
```

`methods` bölümüne ekle:

```js
async loadRenderHistory() {
  try {
    const info = await api.renders.list(this.projectId);
    this.renderHistory = info.history || [];
  } catch {}
},

formatRenderDate(filename) {
  // "render_20260602_143022.mp4" → "02 Haz 2026 14:30"
  const m = filename.match(/render_(\d{4})(\d{2})(\d{2})_(\d{2})(\d{2})/);
  if (!m) return filename;
  return `${m[3]}.${m[2]}.${m[1]} ${m[4]}:${m[5]}`;
},
```

`computed` içindeki `projectId` getter'ı zaten var; yoksa ekle:

```js
projectId() { return store.project.id; },
```

- [ ] **Adım 6.5: Manuel test**

```bash
docker compose up --build
```

Bir render tamamlandıktan sonra "Render Again" butonu altında tarihli render listesi görünmeli; indirme linkleri çalışmalı.

- [ ] **Adım 6.6: Commit**

```bash
git add services/renderer/worker.py services/api/src/routes/renders.js \
        services/web/src/api.js services/web/src/App.vue
git commit -m "feat: render history — worker saves last 5 timestamped copies, API lists them, UI shows download links"
```

---

## Self-Review Kontrol

**Spec kapsamı:**
- [x] Easing mapping düzeltmeleri → Görev 1
- [x] WebSocket render takibi → Görev 2
- [x] UI bileşen test altyapısı → Görev 3
- [x] Template/preset kütüphanesi → Görev 4
- [x] Paralel render worker → Görev 5
- [x] Render geçmişi → Görev 6

**Ek not:** `RenderPanel.vue:93`'te `store.project.elements` kullanılıyor ancak store `store.project.objects` kullanır. Bu mevcut bir bug'dır; Phase 1 kapsamı dışında, ama dikkat edilmeli.

**Tüm görevler tamamlandıktan sonra son commit:**

```bash
git tag v1.2.0-phase1
```
