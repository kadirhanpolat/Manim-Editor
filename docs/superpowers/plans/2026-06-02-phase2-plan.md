# Teknik Borç Temizliği + Phase 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3 teknik borcu temizle, ardından Manim Motion Editor'a fonksiyon grafiği, AnimationGroup/LaggedStart, path animasyonu ve kamera animasyonu özelliklerini ekle.

**Architecture:** İki dalga: Dalga 1 (Task 1-3) tamamen bağımsız olup paralel çalışır; Dalga 2 (Task 4-7) Dalga 1 tamamlandıktan sonra paralel çalışır. Her task kendi branch'inde çalışmalıdır.

**Tech Stack:** Vue 2.7, Konva.js, Node.js/Express, Python Manim CE, Redis, Vitest + Vue Test Utils

**Test commands:**
- Unit tests: `cd services/web && npm run test:unit`
- Engine tests: `cd services/web && npm test`

---

## DALGA 1 — Teknik Borçlar (Task 1, 2, 3 paralel çalışır)

---

### Task 1: RenderPanel hasElements Bug Fix

**Files:**
- Modify: `services/web/src/components/render/RenderPanel.vue:93`

- [ ] **Step 1: Satır 93'ü düzelt**

`services/web/src/components/render/RenderPanel.vue` dosyasında:

```js
// Önceki (yanlış)
hasElements() { return store.project.elements.length > 0; },

// Sonraki (doğru)
hasElements() { return store.project.objects.length > 0; },
```

- [ ] **Step 2: Unit testleri çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass (9 unit tests green).

- [ ] **Step 3: Commit**

```bash
git add services/web/src/components/render/RenderPanel.vue
git commit -m "fix: RenderPanel hasElements uses store.project.objects"
```

---

### Task 2: api.js renders.list Deduplication

**Files:**
- Modify: `services/web/src/api.js`
- Modify: `services/web/src/components/render/RenderPanel.vue` (checkExistingRender method)

- [ ] **Step 1: api.js'ten renders.list'i kaldır**

`services/web/src/api.js` dosyasında renders bölümünü güncelle:

```js
// ─── Renders ──────────────────────────────────────────────────────────────────

export const renders = {
  getLatestUrl: (projectId) => `${API_BASE}/renders/${projectId}/latest.mp4?t=${Date.now()}`,
  getInfo:      (projectId) => request(`/renders/${projectId}`),
};
```

- [ ] **Step 2: RenderPanel.vue'daki doğrudan fetch çağrısını renders.getInfo ile değiştir**

`services/web/src/components/render/RenderPanel.vue` dosyasında, önce import satırını kontrol et. Dosyanın script bölümüne bakarak `api` veya `renders` import'u olup olmadığını gör. Eğer yoksa import ekle:

```js
import { renders } from '../../api.js';
```

Sonra `checkExistingRender` metodunu güncelle:

```js
async checkExistingRender() {
  if (!this.projectId) return;
  try {
    const info = await renders.getInfo(this.projectId);
    this.hasRender = info.hasLatest;
  } catch { /* ignore */ }
},
```

- [ ] **Step 3: Tüm projede `renders.list` referansı kalmadığını doğrula**

```bash
grep -r "renders\.list" services/web/src/
```

Expected: No output (no remaining references).

- [ ] **Step 4: Unit testleri çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass.

- [ ] **Step 5: Commit**

```bash
git add services/web/src/api.js services/web/src/components/render/RenderPanel.vue
git commit -m "fix: remove duplicate renders.list, use renders.getInfo everywhere"
```

---

### Task 3: templates/index.js uid() Unification

**Files:**
- Modify: `services/web/src/store/project.js` (uid'yi export et)
- Modify: `services/web/src/templates/index.js` (yerel uid kaldır, store'dan import et)

- [ ] **Step 1: store/project.js'te uid'yi export et**

`services/web/src/store/project.js` dosyasında (satır 120 civarı):

```js
// Önceki
let _counter = 0;
function uid(prefix = 'id') {
  _counter++;
  return `${prefix}_${Date.now().toString(36)}_${_counter}`;
}

// Sonraki
let _counter = 0;
export function uid(prefix = 'id') {
  _counter++;
  return `${prefix}_${Date.now().toString(36)}_${_counter}`;
}
```

- [ ] **Step 2: templates/index.js'i güncelle**

`services/web/src/templates/index.js` dosyasının başına import ekle ve yerel uid'yi kaldır:

```js
import { uid } from '../store/project.js';

const STAGE = { width: 1920, height: 1080, backgroundColor: '#0f0f0f', backgroundOpacity: 1, backgroundImage: null, gridVisible: true, gridSize: 8, gridColor: '#ffffff', gridOpacity: 0.12, snapEnabled: true, snapToGrid: true, snapToCenter: true, snapToObjects: false };

export const TEMPLATES = [
  // ... (geri kalan içerik aynı kalır, sadece baştaki uid() fonksiyonu kaldırıldı)
```

Dosyanın ilk 3 satırını şu şekilde değiştir (eski `function uid(...)` bloğunu sil):

```js
import { uid } from '../store/project.js';
```

- [ ] **Step 3: Unit testleri çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All 9 tests pass. TEMPLATES testi ID format değişikliğinden etkilenmez çünkü sadece `id` varlığını kontrol eder, formatını değil.

- [ ] **Step 4: Commit**

```bash
git add services/web/src/store/project.js services/web/src/templates/index.js
git commit -m "fix: templates use store uid() instead of Math.random-based local uid"
```

---

## DALGA 2 — Phase 2 Özellikleri (Task 4, 5, 6, 7 paralel çalışır — Dalga 1 tamamlandıktan sonra)

Dalga 2'ye başlamadan önce Dalga 1'in tüm branch'lerini main'e merge et.

---

### Task 4: Fonksiyon Grafiği / NumberPlane

**Files:**
- Modify: `services/web/src/store/project.js`
- Modify: `services/web/src/components/inspector/PropertiesPanel.vue`
- Modify: `services/web/src/components/stage/StageCanvas.vue`
- Modify: `services/api/src/compiler/codegen.js`
- Create: `services/web/tests/components/graphs.test.js`

#### 4A: Store şeması ve action'lar

- [ ] **Step 4A-1: SHAPE_DEFAULTS ve SHAPE_COLORS'a numberplane ve numberline ekle**

`services/web/src/store/project.js` dosyasında (satır 171 civarı, SHAPE_DEFAULTS nesnesi):

```js
export const SHAPE_DEFAULTS = {
  rectangle:  { width: 160, height: 100, fill: '#3b82f6', stroke: '#fff',  strokeWidth: 2 },
  square:     { width: 120, height: 120, fill: '#3b82f6', stroke: '#fff',  strokeWidth: 2 },
  circle:     { width: 120, height: 120, fill: '#22c55e', stroke: '#fff',  strokeWidth: 2 },
  ellipse:    { width: 160, height: 100, fill: '#06b6d4', stroke: '#fff',  strokeWidth: 2 },
  triangle:   { width: 120, height: 120, fill: '#f59e0b', stroke: '#fff',  strokeWidth: 2 },
  star:       { width: 120, height: 120, fill: '#eab308', stroke: '#fff',  strokeWidth: 2 },
  polygon:    { width: 120, height: 120, fill: '#8b5cf6', stroke: '#fff',  strokeWidth: 2 },
  line:       { width: 200, height: 4,   fill: '#94a3b8', stroke: '#94a3b8', strokeWidth: 3 },
  arrow:      { width: 200, height: 40,  fill: '#ef4444', stroke: '#fff',  strokeWidth: 2 },
  heart:      { width: 120, height: 120, fill: '#ec4899', stroke: '#fff',  strokeWidth: 2 },
  dot:        { width: 20,  height: 20,  fill: '#ffffff', stroke: 'transparent', strokeWidth: 0 },
  dot_grid:   { width: 200, height: 200, fill: '#a855f7', stroke: 'transparent', strokeWidth: 0 },
  text:       { width: 200, height: 50,  fill: '#ffffff', stroke: 'transparent', strokeWidth: 0 },
  image:      { width: 200, height: 200, fill: 'transparent', stroke: 'transparent', strokeWidth: 0 },
  svg_asset:  { width: 200, height: 200, fill: 'transparent', stroke: 'transparent', strokeWidth: 0 },
  latex:      { width: 200, height: 80,  fill: '#ffffff', stroke: 'transparent', strokeWidth: 0 },
  axes:       { width: 400, height: 300, fill: '#ffffff', stroke: '#ffffff', strokeWidth: 2 },
  numberplane:{ width: 600, height: 400, fill: '#334155', stroke: '#64748b', strokeWidth: 1 },
  numberline: { width: 500, height: 60,  fill: '#ffffff', stroke: '#ffffff', strokeWidth: 2 },
};
```

SHAPE_COLORS'a da ekle (satır 191 civarı):

```js
export const SHAPE_COLORS = {
  rectangle: '#3b82f6', square: '#3b82f6', circle: '#22c55e', ellipse: '#06b6d4',
  triangle: '#f59e0b', star: '#eab308', polygon: '#8b5cf6',
  line: '#94a3b8', arrow: '#ef4444',
  heart: '#ec4899', dot: '#94a3b8', dot_grid: '#a855f7',
  text: '#f472b6', image: '#f59e0b', svg_asset: '#f59e0b',
  latex: '#a855f7', axes: '#10b981', numberplane: '#334155', numberline: '#10b981'
};
```

- [ ] **Step 4A-2: addObject'te axes, numberplane ve numberline için özel alanlar ekle**

`services/web/src/store/project.js` dosyasında `addObject` action'ında (satır 307-313 civarı):

```js
// Önceki
...(type === 'axes' ? { xRange: [-5, 5, 1], yRange: [-3, 3, 1] } : {}),

// Sonraki
...(type === 'axes'        ? { xRange: [-5, 5, 1], yRange: [-3, 3, 1], graphs: [] } : {}),
...(type === 'numberplane' ? { xRange: [-5, 5, 1], yRange: [-3, 3, 1], xStep: 1, yStep: 1 } : {}),
...(type === 'numberline'  ? { xRange: [-5, 5, 1] } : {}),
```

nameMap'e de ekle (satır 278 civarı):

```js
const nameMap = {
  dot_grid: 'Dot Grid', svg_asset: 'SVG', rectangle: 'Rectangle',
  ellipse: 'Ellipse', triangle: 'Triangle', star: 'Star',
  polygon: 'Polygon', line: 'Line', arrow: 'Arrow', text: 'Text',
  latex: 'LaTeX', axes: 'Axes', numberplane: 'NumberPlane', numberline: 'NumberLine'
};
```

- [ ] **Step 4A-3: addGraph ve removeGraph action'larını ekle**

`services/web/src/store/project.js` dosyasında, actions nesnesine (actions.addClip'ten önce uygun bir yere) ekle:

```js
addGraph(objId, graphData = {}) {
  const obj = getters.objectById(objId);
  if (!obj || obj.type !== 'axes') return null;
  if (!obj.graphs) Vue.set(obj, 'graphs', []);
  const graph = {
    id: uid('graph'),
    expression: graphData.expression || 'x**2',
    color: graphData.color || '#f59e0b',
    xMin: graphData.xMin ?? (obj.xRange?.[0] ?? -5),
    xMax: graphData.xMax ?? (obj.xRange?.[1] ?? 5),
    strokeWidth: graphData.strokeWidth || 3,
  };
  obj.graphs.push(graph);
  store.isDirty = true;
  actions.commitState();
  return graph;
},

removeGraph(objId, graphId) {
  const obj = getters.objectById(objId);
  if (!obj || !obj.graphs) return;
  const idx = obj.graphs.findIndex(g => g.id === graphId);
  if (idx !== -1) {
    obj.graphs.splice(idx, 1);
    store.isDirty = true;
    actions.commitState();
  }
},

updateGraph(objId, graphId, updates) {
  const obj = getters.objectById(objId);
  if (!obj || !obj.graphs) return;
  const graph = obj.graphs.find(g => g.id === graphId);
  if (!graph) return;
  for (const key of Object.keys(updates)) Vue.set(graph, key, updates[key]);
  store.isDirty = true;
},
```

#### 4B: PropertiesPanel.vue — Graph UI

- [ ] **Step 4B-1: axes seçiliyken grafik paneli ekle**

`services/web/src/components/inspector/PropertiesPanel.vue` dosyasında, axes için özel bir bölüm ekle. Dosyada mevcut axes bölümünün ardından (veya axes range alanlarının arkasına) ekle.

Önce script bölümünde imports'a store actions ekle (actions henüz import edilmemişse):

```js
import { store, actions, getters } from '../../store/project.js';
```

Template'te, axes tipine özgü bir section ekle. Mevcut PropertiesPanel'de `<!-- Rotation -->` bölümünden önce veya başka bir uygun yerde:

```html
<!-- Axes: Graph Functions -->
<Section v-if="obj && obj.type === 'axes'" label="Graphs">
  <div v-for="graph in (obj.graphs || [])" :key="graph.id" class="mb-2 p-2 rounded bg-studio-surface2 border border-studio-border">
    <div class="flex items-center gap-1 mb-1">
      <input
        class="input input-sm flex-1 font-mono text-xs"
        :value="graph.expression"
        placeholder="x**2"
        @change="updateGraph(graph.id, 'expression', $event.target.value)"
      />
      <input
        type="color"
        class="w-6 h-6 rounded cursor-pointer border-0 bg-transparent"
        :value="graph.color"
        @input="updateGraph(graph.id, 'color', $event.target.value)"
      />
      <button class="text-studio-error hover:opacity-80 text-xs px-1" @click="removeGraph(graph.id)">✕</button>
    </div>
    <div class="grid grid-cols-2 gap-1">
      <Num label="x min" :value="graph.xMin" @input="updateGraph(graph.id, 'xMin', $event)" />
      <Num label="x max" :value="graph.xMax" @input="updateGraph(graph.id, 'xMax', $event)" />
    </div>
  </div>
  <button
    class="w-full mt-1 py-1 text-xs rounded border border-dashed border-studio-accent/50 text-studio-accent hover:bg-studio-accent/10"
    @click="addGraph"
  >+ Add Graph</button>
</Section>
```

Script bölümünde `methods` içine:

```js
addGraph() {
  if (!this.obj || this.obj.type !== 'axes') return;
  actions.addGraph(this.obj.id);
},
removeGraph(graphId) {
  if (!this.obj) return;
  actions.removeGraph(this.obj.id, graphId);
},
updateGraph(graphId, key, value) {
  if (!this.obj) return;
  actions.updateGraph(this.obj.id, graphId, { [key]: value });
},
```

#### 4C: StageCanvas.vue — Graph Preview

- [ ] **Step 4C-1: Axes grup içine grafik önizlemesi ekle**

`services/web/src/components/stage/StageCanvas.vue` dosyasında, axes v-group bloğunu bul (satır 74-84):

```html
<!-- Axes -->
<v-group v-if="obj.type === 'axes' && isVis(obj.id)" :key="obj.id" :config="groupCfg(obj)" @mousedown="onObjDown(obj.id, $event)" @dragend="onDragEnd(obj.id, $event)" @transform="onTransform(obj.id, $event)" @transformend="onTransformEnd(obj.id, $event)">
  <v-rect :config="axesBgCfg(obj)" />
  <v-line :config="axesXLineCfg(obj)" />
  <v-line :config="axesYLineCfg(obj)" />
  <v-line :config="axesXArrowCfg(obj)" />
  <v-line :config="axesYArrowCfg(obj)" />
  <v-line v-for="(tick, ti) in axesXTicks(obj)" :key="'xt'+ti" :config="tick" />
  <v-line v-for="(tick, ti) in axesYTicks(obj)" :key="'yt'+ti" :config="tick" />
  <v-text :config="axesLabelCfg(obj, 'x')" />
  <v-text :config="axesLabelCfg(obj, 'y')" />
  <!-- Graph curves -->
  <v-line v-for="(gc, gi) in axesGraphCurves(obj)" :key="'gc'+gi" :config="gc" />
</v-group>
```

Script bölümünde `methods` içine yeni yardımcı metod ekle:

```js
axesGraphCurves(obj) {
  if (!obj.graphs || obj.graphs.length === 0) return [];
  const curves = [];
  const xr = obj.xRange || [-5, 5, 1];
  const yr = obj.yRange || [-3, 3, 1];
  const xMin = xr[0], xMax = xr[1];
  const yMin = yr[0], yMax = yr[1];
  const pw = obj.width;
  const ph = obj.height;

  for (const graph of obj.graphs) {
    let fn;
    try {
      // eslint-disable-next-line no-new-func
      fn = new Function('x', `"use strict"; return (${graph.expression});`);
    } catch { continue; }

    const steps = 80;
    const points = [];
    for (let i = 0; i <= steps; i++) {
      const x = xMin + (xMax - xMin) * (i / steps);
      let y;
      try { y = fn(x); } catch { continue; }
      if (!Number.isFinite(y)) continue;
      // Map math coords to canvas coords
      const cx = ((x - xMin) / (xMax - xMin)) * pw - pw / 2;
      const cy = -((y - yMin) / (yMax - yMin)) * ph + ph / 2;
      points.push(cx, cy);
    }
    if (points.length >= 4) {
      curves.push({
        points,
        stroke: graph.color || '#f59e0b',
        strokeWidth: graph.strokeWidth || 3,
        listening: false,
        tension: 0.3,
      });
    }
  }
  return curves;
},
```

#### 4D: codegen.js — Axes, NumberPlane, NumberLine

- [ ] **Step 4D-1: codegen.js'te axes case'ini güncelle, numberplane ve numberline ekle**

`services/api/src/compiler/codegen.js` dosyasında `objectCode` fonksiyonundaki switch bloğunda:

```js
case 'axes': {
  const xr = obj.xRange || [-5, 5, 1];
  const yr = obj.yRange || [-3, 3, 1];
  lines.push(`${n} = Axes(x_range=[${xr[0]}, ${xr[1]}, ${xr[2]}], y_range=[${yr[0]}, ${yr[1]}, ${yr[2]}], x_length=${(obj.width / sw * 14).toFixed(1)}, y_length=${(obj.height / sh * 8).toFixed(1)}, tips=True)`);
  // Graphs
  if (obj.graphs && obj.graphs.length > 0) {
    for (const g of obj.graphs) {
      const gn = `${n}_graph_${g.id.replace(/[^a-zA-Z0-9_]/g, '_')}`;
      const col = hex(g.color) || '"#F59E0B"';
      const xMin = g.xMin ?? xr[0];
      const xMax = g.xMax ?? xr[1];
      lines.push(`${gn} = ${n}.plot(lambda x: ${g.expression || 'x**2'}, x_range=[${xMin}, ${xMax}], color=${col}, stroke_width=${g.strokeWidth || 3})`);
    }
  }
  break;
}
case 'numberplane': {
  const xr = obj.xRange || [-5, 5, 1];
  const yr = obj.yRange || [-3, 3, 1];
  const xs = obj.xStep || 1;
  const ys = obj.yStep || 1;
  lines.push(`${n} = NumberPlane(x_range=[${xr[0]}, ${xr[1]}, ${xs}], y_range=[${yr[0]}, ${yr[1]}, ${ys}], x_length=${(obj.width / sw * 14).toFixed(1)}, y_length=${(obj.height / sh * 8).toFixed(1)})`);
  break;
}
case 'numberline': {
  const xr = obj.xRange || [-5, 5, 1];
  lines.push(`${n} = NumberLine(x_range=[${xr[0]}, ${xr[1]}, ${xr[2]}], length=${(obj.width / sw * 14).toFixed(1)})`);
  break;
}
```

Ayrıca codegen'in `generatePythonCode` fonksiyonunda graph nesnelerini `self.add()`'e eklemek gerekiyor. `# Object definitions` bölümünde, her axes nesnesinden sonra graph'larını da add et:

```js
// Object definitions
const oMap = {};
L.push(`${indent}# Objects`);
for (const obj of project.objects) {
  oMap[obj.id] = obj;
  objectCode(obj, sw, sh, assetsPath, assetMap).forEach(l => L.push(indent + l));
  // Axes: add graph objects to scene
  if (obj.type === 'axes' && obj.graphs && obj.graphs.length > 0) {
    const nn = vn(obj.id);
    for (const g of obj.graphs) {
      const gn = `${nn}_graph_${g.id.replace(/[^a-zA-Z0-9_]/g, '_')}`;
      L.push(`${indent}self.add(${gn})`);
    }
  }
  L.push('');
}
```

#### 4E: Test

- [ ] **Step 4E-1: Test dosyası oluştur**

`services/web/tests/components/graphs.test.js` dosyasını oluştur:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { store, actions, getters } from '../../src/store/project.js';

beforeEach(() => {
  actions.newProject('Test', 'visual');
  actions.commitState();
});

describe('addObject axes graphs field', () => {
  it('axes object gets empty graphs array', () => {
    const obj = actions.addObject('axes', 960, 540);
    expect(obj.type).toBe('axes');
    expect(Array.isArray(obj.graphs)).toBe(true);
    expect(obj.graphs).toHaveLength(0);
  });

  it('numberplane object gets xRange and yRange', () => {
    const obj = actions.addObject('numberplane', 960, 540);
    expect(obj.type).toBe('numberplane');
    expect(obj.xRange).toBeDefined();
    expect(obj.yRange).toBeDefined();
  });

  it('numberline object gets xRange', () => {
    const obj = actions.addObject('numberline', 960, 540);
    expect(obj.type).toBe('numberline');
    expect(obj.xRange).toBeDefined();
  });
});

describe('addGraph / removeGraph', () => {
  it('adds a graph to an axes object', () => {
    const obj = actions.addObject('axes', 960, 540);
    const graph = actions.addGraph(obj.id, { expression: 'x**2', color: '#ff0000' });
    expect(graph).not.toBeNull();
    expect(graph.expression).toBe('x**2');
    const updated = getters.objectById(obj.id);
    expect(updated.graphs).toHaveLength(1);
  });

  it('removes a graph from an axes object', () => {
    const obj = actions.addObject('axes', 960, 540);
    const graph = actions.addGraph(obj.id, { expression: 'x**2' });
    actions.removeGraph(obj.id, graph.id);
    const updated = getters.objectById(obj.id);
    expect(updated.graphs).toHaveLength(0);
  });

  it('returns null when adding graph to non-axes object', () => {
    const obj = actions.addObject('circle', 960, 540);
    const result = actions.addGraph(obj.id, { expression: 'x**2' });
    expect(result).toBeNull();
  });
});
```

- [ ] **Step 4E-2: Testi çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass including new graphs tests.

- [ ] **Step 4E-3: Commit**

```bash
git add services/web/src/store/project.js \
        services/web/src/components/inspector/PropertiesPanel.vue \
        services/web/src/components/stage/StageCanvas.vue \
        services/api/src/compiler/codegen.js \
        services/web/tests/components/graphs.test.js
git commit -m "feat: function graphs / NumberPlane / NumberLine (Phase 2 feature 1)"
```

---

### Task 5: AnimationGroup / LaggedStart

**Files:**
- Modify: `services/web/src/store/project.js` (clip defaults)
- Modify: `services/web/src/components/timeline/TimelineClip.vue`
- Modify: `services/api/src/compiler/codegen.js`
- Create: `services/web/tests/components/parallel-clips.test.js`

#### 5A: Store — clip'e parallel ve lag_ratio ekle

- [ ] **Step 5A-1: addClip defaults'ına parallel ve lag_ratio ekle**

`services/web/src/store/project.js` dosyasında `addClip` action'ında (satır 510 civarı), clip nesnesine iki yeni alan ekle:

```js
const clip = {
  id: uid('clip'), type: 'transform', startTime: 0, duration: 1.5,
  easing: 'ease_in_out', sourceId: null, targetId: null, params: {},
  overshoot: 0, settle: 1.0, morphQuality: 'medium',
  parallel: false, lag_ratio: 0,
  ...clipData
};
```

#### 5B: TimelineClip.vue — Paralel gösterge

- [ ] **Step 5B-1: Paralel kliplere görsel gösterge ekle**

`services/web/src/components/timeline/TimelineClip.vue` dosyasını aç. Klip başlığını veya clip container'ını gösteren template kısmını bul. Clip içeriğinde, `clip.parallel === true` olduğunda bir badge göster:

Mevcut clip template'inin içine (klip adı veya type gösteriminin yanına) ekle:

```html
<span v-if="clip.parallel" class="inline-block ml-1 px-1 text-[8px] rounded bg-violet-500/30 text-violet-300 leading-none align-middle" title="Parallel (AnimationGroup)">∥</span>
```

Clip inspector'ında (eğer bu dosyada varsa, yoksa AnimationPanel.vue'da) parallel toggle ve lag_ratio input ekle:

```html
<label class="flex items-center gap-2 text-xs text-studio-text-muted mt-2">
  <input type="checkbox" :checked="clip.parallel" @change="updateClip({ parallel: $event.target.checked })" />
  Parallel (AnimationGroup)
</label>
<div v-if="clip.parallel" class="mt-1">
  <Num label="Lag Ratio" :value="clip.lag_ratio || 0" :min="0" :max="1" :step="0.1" @input="updateClip({ lag_ratio: $event })" />
</div>
```

`updateClip` metodunu script bölümüne ekle (varsa güncelle):

```js
updateClip(updates) {
  if (!this.clip) return;
  actions.updateClip(this.clip.id, updates);
},
```

#### 5C: codegen.js — AnimationGroup / LaggedStart çıktısı

- [ ] **Step 5C-1: generatePythonCode'da clip step üretimini güncelle**

`services/api/src/compiler/codegen.js` dosyasında, "Clip animations" bölümünü (satır 378 civarı) güncelle. Önce clip'leri tek tek işleyen for döngüsünü bul ve paralel gruplamayı ekle:

```js
// Clip animations — parallel clips are grouped into AnimationGroup/LaggedStart
// Group consecutive clips with the same startTime where parallel=true
const clipGroups = [];
let i = 0;
while (i < clips.length) {
  const c = clips[i];
  if (c.parallel) {
    // Find all clips in this parallel group (same startTime)
    const group = [c];
    let j = i + 1;
    while (j < clips.length && clips[j].parallel && Math.abs(clips[j].startTime - c.startTime) < 0.01) {
      group.push(clips[j]);
      j++;
    }
    clipGroups.push({ type: 'group', clips: group, startTime: c.startTime });
    i = j;
  } else {
    clipGroups.push({ type: 'single', clip: c, startTime: c.startTime });
    i++;
  }
}

for (const cg of clipGroups) {
  if (cg.type === 'single') {
    const c = cg.clip;
    const sn = vn(c.sourceId);
    const dur = c.duration;
    const rtStr = rtOpt(dur);
    const rfStr = rfOpt(c.easing);
    let code;
    switch (c.type) {
      case 'transform': {
        const tn = vn(c.targetId);
        const srcObj = oMap[c.sourceId], tgtObj = oMap[c.targetId];
        const hasRaster = ['image', 'svg_asset'].includes(srcObj?.type) || ['image', 'svg_asset'].includes(tgtObj?.type);
        const anim = hasRaster ? 'FadeTransform' : 'ReplacementTransform';
        code = `self.play(${anim}(${sn}, ${tn})${rtStr}${rfStr})`;
        break;
      }
      case 'move': {
        const mp = stageToManim(c.params?.targetX || 0, c.params?.targetY || 0, sw, sh);
        code = `self.play(${sn}.animate.move_to([${mp.x.toFixed(2)}, ${mp.y.toFixed(2)}, 0])${rtStr}${rfStr})`;
        break;
      }
      case 'scale':
        code = `self.play(${sn}.animate.scale(${(c.params?.targetScaleX || 1).toFixed(2)})${rtStr}${rfStr})`;
        break;
      case 'fade': {
        const op = c.params?.targetOpacity ?? 0;
        code = op < 0.01
          ? `self.play(FadeOut(${sn})${rtStr}${rfStr})`
          : `self.play(${sn}.animate.set_opacity(${op.toFixed(2)})${rtStr}${rfStr})`;
        break;
      }
      case 'rotate': {
        const ang = ((c.params?.targetRotation || 360) - (oMap[c.sourceId]?.rotation || 0)) * Math.PI / 180;
        code = `self.play(Rotate(${sn}, angle=${ang.toFixed(2)})${rtStr}${rfStr})`;
        break;
      }
    }
    if (code) steps.push({ time: c.startTime, order: 1, code, dur });
  } else {
    // Parallel group: AnimationGroup or LaggedStart
    const groupClips = cg.clips;
    const dur = Math.max(...groupClips.map(c => c.duration));
    const rtStr = rtOpt(dur);
    const maxLag = Math.max(...groupClips.map(c => c.lag_ratio || 0));

    const animExprs = groupClips.map(c => {
      const sn = vn(c.sourceId);
      switch (c.type) {
        case 'move': {
          const mp = stageToManim(c.params?.targetX || 0, c.params?.targetY || 0, sw, sh);
          return `${sn}.animate.move_to([${mp.x.toFixed(2)}, ${mp.y.toFixed(2)}, 0])`;
        }
        case 'scale':
          return `${sn}.animate.scale(${(c.params?.targetScaleX || 1).toFixed(2)})`;
        case 'fade': {
          const op = c.params?.targetOpacity ?? 0;
          return op < 0.01 ? `FadeOut(${sn})` : `${sn}.animate.set_opacity(${op.toFixed(2)})`;
        }
        case 'rotate': {
          const ang = ((c.params?.targetRotation || 360) - (oMap[c.sourceId]?.rotation || 0)) * Math.PI / 180;
          return `Rotate(${sn}, angle=${ang.toFixed(2)})`;
        }
        case 'transform': {
          const tn = vn(c.targetId);
          const srcObj = oMap[c.sourceId], tgtObj = oMap[c.targetId];
          const hasRaster = ['image', 'svg_asset'].includes(srcObj?.type) || ['image', 'svg_asset'].includes(tgtObj?.type);
          return hasRaster ? `FadeTransform(${sn}, ${tn})` : `ReplacementTransform(${sn}, ${tn})`;
        }
        default: return null;
      }
    }).filter(Boolean);

    if (animExprs.length > 0) {
      const animList = animExprs.join(', ');
      const groupFn = maxLag > 0 ? `LaggedStart` : `AnimationGroup`;
      const lagStr = maxLag > 0 ? `, lag_ratio=${maxLag.toFixed(2)}` : '';
      const code = `self.play(${groupFn}(${animList}${lagStr})${rtStr})`;
      steps.push({ time: cg.startTime, order: 1, code, dur });
    }
  }
}
```

**Önemli:** Yukarıdaki kod, mevcut `for (const c of clips)` döngüsünün **yerine** geçer. Eski döngüyü sil.

#### 5D: Test

- [ ] **Step 5D-1: Test dosyası oluştur**

`services/web/tests/components/parallel-clips.test.js`:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { store, actions } from '../../src/store/project.js';

beforeEach(() => {
  actions.newProject('Test', 'visual');
  actions.commitState();
});

describe('parallel clip defaults', () => {
  it('new clips have parallel=false and lag_ratio=0', () => {
    const obj = actions.addObject('circle', 960, 540);
    const clip = actions.addClip(0, {
      type: 'move', sourceId: obj.id, startTime: 0, duration: 1,
      params: { targetX: 100, targetY: 100 }
    });
    expect(clip.parallel).toBe(false);
    expect(clip.lag_ratio).toBe(0);
  });

  it('clip can be set to parallel', () => {
    const obj = actions.addObject('circle', 960, 540);
    const clip = actions.addClip(0, {
      type: 'move', sourceId: obj.id, startTime: 0, duration: 1,
      params: { targetX: 100, targetY: 100 }, parallel: true, lag_ratio: 0.2
    });
    expect(clip.parallel).toBe(true);
    expect(clip.lag_ratio).toBe(0.2);
  });
});
```

- [ ] **Step 5D-2: Testi çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass.

- [ ] **Step 5D-3: Commit**

```bash
git add services/web/src/store/project.js \
        services/web/src/components/timeline/TimelineClip.vue \
        services/api/src/compiler/codegen.js \
        services/web/tests/components/parallel-clips.test.js
git commit -m "feat: AnimationGroup / LaggedStart via parallel clip flag (Phase 2 feature 2)"
```

---

### Task 6: Path Animasyonu (MoveAlongPath)

**Files:**
- Modify: `services/web/src/store/project.js`
- Modify: `services/web/src/components/stage/StageCanvas.vue`
- Modify: `services/web/src/engine/playback.js`
- Modify: `services/api/src/compiler/codegen.js`
- Create: `services/web/tests/components/path-move.test.js`

#### 6A: Store — path_move clip type

- [ ] **Step 6A-1: addPathMoveClip action ekle**

`services/web/src/store/project.js` dosyasında actions nesnesine ekle:

```js
addPathMoveClip(sourceId, pathPoints, trackIndex = 0) {
  if (!sourceId || !pathPoints || pathPoints.length < 2) return null;
  const clip = actions.addClip(trackIndex, {
    type: 'path_move',
    sourceId,
    startTime: 0,
    duration: 2.0,
    easing: 'ease_in_out',
    path: pathPoints,   // [{x, y}, ...]
    params: {},
  });
  return clip;
},
```

#### 6B: playback.js — path_move klip desteği

- [ ] **Step 6B-1: _evaluateClip metoduna path_move case ekle**

`services/web/src/engine/playback.js` dosyasında `_evaluateClip` metodunda, mevcut `switch (clip.type)` bloğuna yeni case ekle:

```js
case 'path_move': {
  if (!clip.path || clip.path.length < 2) break;
  // Compute total arc length
  const pts = clip.path;
  let totalLen = 0;
  const segLens = [];
  for (let k = 1; k < pts.length; k++) {
    const dx = pts[k].x - pts[k-1].x;
    const dy = pts[k].y - pts[k-1].y;
    const len = Math.sqrt(dx*dx + dy*dy);
    segLens.push(len);
    totalLen += len;
  }
  // Find position at easedT along path
  let target = easedT * totalLen;
  let cumLen = 0;
  let px = pts[0].x, py = pts[0].y;
  for (let k = 0; k < segLens.length; k++) {
    if (cumLen + segLens[k] >= target) {
      const t2 = (target - cumLen) / segLens[k];
      px = lerp(pts[k].x, pts[k+1].x, t2);
      py = lerp(pts[k].y, pts[k+1].y, t2);
      break;
    }
    cumLen += segLens[k];
    if (k === segLens.length - 1) {
      px = pts[pts.length - 1].x;
      py = pts[pts.length - 1].y;
    }
  }
  overrides.x = px;
  overrides.y = py;
  break;
}
```

Bu case'i mevcut switch bloğuna, `case 'rotate':` bloğundan sonra ekle.

#### 6C: StageCanvas.vue — Path çizim aracı

- [ ] **Step 6C-1: Araç durumu ve tıklama işleyicileri ekle**

`services/web/src/components/stage/StageCanvas.vue` dosyasında script bölümünde, data() içine yeni state ekle:

```js
pathDrawing: false,        // path draw modu aktif mi
pathPoints: [],            // çizilen noktalar [{x, y}]
pathSourceId: null,        // hangi nesne için path çiziliyor
```

Template'te, v-stage üzerine `@dblclick="onStageDblClick"` event ekle:

```html
<v-stage ref="konvaStage" :config="stageConfig"
  @mousedown="handleStageMouseDown"
  @dblclick="onStageDblClick"
  @wheel="handleWheel">
```

Template'te, path önizlemesi için bir katman ekle (morph layer'dan sonra):

```html
<!-- Path draw preview layer -->
<v-layer v-if="pathDrawing">
  <v-line v-if="pathPoints.length >= 2" :config="{
    points: pathPoints.flatMap(p => [p.x - obj_x_offset, p.y - obj_y_offset]),
    stroke: '#a855f7',
    strokeWidth: 2,
    dash: [6, 3],
    listening: false,
  }" />
  <v-circle v-for="(pt, pi) in pathPoints" :key="'pp'+pi" :config="{
    x: pt.x, y: pt.y, radius: 4, fill: '#a855f7', listening: false
  }" />
</v-layer>
```

**Not:** Konva koordinat sistemi ile proje koordinat sistemi arasında dönüşüm gerekebilir. `pathPoints` proje koordinatlarında saklanır.

Methods bölümüne ekle:

```js
startPathDraw(sourceId) {
  this.pathDrawing = true;
  this.pathPoints = [];
  this.pathSourceId = sourceId;
},

onStageDblClick(e) {
  if (!this.pathDrawing || this.pathPoints.length < 2) {
    if (this.pathDrawing) this.pathDrawing = false;
    return;
  }
  // Finish path: create clip
  actions.addPathMoveClip(this.pathSourceId, [...this.pathPoints]);
  this.pathDrawing = false;
  this.pathPoints = [];
  this.pathSourceId = null;
},
```

`handleStageMouseDown` metoduna path drawing mantığı ekle (mevcut metodun başına):

```js
handleStageMouseDown(e) {
  if (this.pathDrawing) {
    // Add point to path (in project coordinates)
    const stage = this.$refs.konvaStage.getStage();
    const pos = stage.getPointerPosition();
    const transform = stage.getAbsoluteTransform().copy();
    transform.invert();
    const stagePos = transform.point(pos);
    // Convert from canvas coordinates to project coordinates
    const projectX = (stagePos.x / this.stageConfig.width) * this.project.stage.width;
    const projectY = (stagePos.y / this.stageConfig.height) * this.project.stage.height;
    this.pathPoints.push({ x: Math.round(projectX), y: Math.round(projectY) });
    return;
  }
  // ... existing mousedown logic
}
```

**Not:** Bu method'un doğru entegrasyonu için mevcut `handleStageMouseDown` kodunu incele. Eğer farklı parametre isimleri kullanıyorsa (örn. `stageConfig.width` yerine `scaleX` vs.) mevcut kodu referans al.

#### 6D: codegen.js — path_move case

- [ ] **Step 6D-1: codegen.js'e path_move case ekle**

`services/api/src/compiler/codegen.js` dosyasında, Task 5'te güncellenen clip işleme kodunda (single clip switch bloğunda), `case 'rotate':` bloğundan sonra ekle:

```js
case 'path_move': {
  if (!c.path || c.path.length < 2) break;
  const pn = `path_${sn}`;
  // Convert project coordinates to Manim coordinates
  const pts = c.path.map(p => {
    const m = stageToManim(p.x, p.y, sw, sh);
    return `[${m.x.toFixed(3)}, ${m.y.toFixed(3)}, 0]`;
  });
  code = [
    `${pn} = VMobject()`,
    `${pn}.set_points_as_corners([np.array(p) for p in [${pts.join(', ')}]])`,
    `self.play(MoveAlongPath(${sn}, ${pn})${rtStr}${rfStr})`,
  ].join(`\n${indent}`);
  break;
}
```

**Not:** Bu case `steps.push(...)` satırından önce geldiği için `code` değişkenini doğru ayarlar. Ancak `code`'un çok satırlı olduğunu fark et — steps emit bölümünde `L.push(indent + step.code)` satırı tüm satırları doğru render etmeyebilir. Adım birden fazla self.play içermeyeceğinden kod doğrudan output edilir; `path_move` için ayrı bir emit mekanizması gerekebilir.

Bunun yerine, `path_move` için `steps`'e birden fazla satır eklemek yerine, tüm kodu tek bir string olarak saklayabiliriz. `steps` array'i `code` string içeriyor, `L.push(indent + step.code)` çalışmasıyla beraber `\n${indent}` içeren kod doğru girintilenir.

#### 6E: Test

- [ ] **Step 6E-1: Test dosyası oluştur**

`services/web/tests/components/path-move.test.js`:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { store, actions } from '../../src/store/project.js';

beforeEach(() => {
  actions.newProject('Test', 'visual');
  actions.commitState();
});

describe('addPathMoveClip', () => {
  it('creates a path_move clip with given points', () => {
    const obj = actions.addObject('circle', 960, 540);
    const pts = [{ x: 100, y: 100 }, { x: 500, y: 300 }, { x: 900, y: 100 }];
    const clip = actions.addPathMoveClip(obj.id, pts);
    expect(clip).not.toBeNull();
    expect(clip.type).toBe('path_move');
    expect(clip.path).toHaveLength(3);
    expect(clip.path[0]).toEqual({ x: 100, y: 100 });
  });

  it('returns null for fewer than 2 points', () => {
    const obj = actions.addObject('circle', 960, 540);
    const result = actions.addPathMoveClip(obj.id, [{ x: 100, y: 100 }]);
    expect(result).toBeNull();
  });
});
```

- [ ] **Step 6E-2: Testi çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass.

- [ ] **Step 6E-3: Commit**

```bash
git add services/web/src/store/project.js \
        services/web/src/components/stage/StageCanvas.vue \
        services/web/src/engine/playback.js \
        services/api/src/compiler/codegen.js \
        services/web/tests/components/path-move.test.js
git commit -m "feat: MoveAlongPath animation (path_move clip type, Phase 2 feature 3)"
```

---

### Task 7: Kamera Animasyonları

**Files:**
- Modify: `services/web/src/store/project.js`
- Modify: `services/web/src/components/toolbar/Topbar.vue`
- Modify: `services/web/src/components/timeline/Timeline.vue`
- Modify: `services/web/src/engine/playback.js`
- Modify: `services/api/src/compiler/codegen.js`
- Create: `services/web/tests/components/camera.test.js`

#### 7A: Store — cameraType ve cameraTrack

- [ ] **Step 7A-1: createDefaultProject'e cameraType ve cameraTrack ekle**

`services/web/src/store/project.js` dosyasında `createDefaultProject` fonksiyonuna (satır 29-58 civarı):

```js
function createDefaultProject(editorMode = 'visual') {
  return {
    id: null,
    name: 'My Animation',
    editorMode,
    codeSource: editorMode === 'code' ? CODE_MODE_TEMPLATE : '',
    stage: {
      width: 1920,
      height: 1080,
      backgroundColor: '#000000',
      backgroundOpacity: 1,
      backgroundImage: null,
      gridVisible: true,
      gridSize: 8,
      gridColor: '#ffffff',
      gridOpacity: 0.12,
      snapEnabled: true,
      snapToGrid: true,
      snapToCenter: true,
      snapToObjects: false
    },
    assets: [],
    objects: [],
    groups: [],
    tracks: [
      { id: 'track_1', name: 'Track 1', clips: [] }
    ],
    sceneDuration: 10,
    cameraType: 'static',   // 'static' | 'moving'
    cameraTrack: [],         // camera_move clips
  };
}
```

- [ ] **Step 7A-2: addCameraMoveClip action ekle**

`services/web/src/store/project.js` dosyasında actions nesnesine ekle:

```js
setCameraType(type) {
  Vue.set(store.project, 'cameraType', type);
  if (!store.project.cameraTrack) Vue.set(store.project, 'cameraTrack', []);
  store.isDirty = true;
  actions.commitState();
},

addCameraMoveClip(params = {}) {
  if (!store.project.cameraTrack) Vue.set(store.project, 'cameraTrack', []);
  const clip = {
    id: uid('cam'),
    type: 'camera_move',
    startTime: params.startTime || 0,
    duration: params.duration || 2.0,
    easing: params.easing || 'ease_in_out',
    params: {
      targetX: params.targetX || 0,
      targetY: params.targetY || 0,
      zoom: params.zoom || 1.0,
    },
  };
  store.project.cameraTrack.push(clip);
  store.isDirty = true;
  actions.commitState();
  return clip;
},

updateCameraClip(clipId, updates) {
  const clip = store.project.cameraTrack?.find(c => c.id === clipId);
  if (!clip) return;
  if (updates.params) {
    for (const k of Object.keys(updates.params)) Vue.set(clip.params, k, updates.params[k]);
  }
  for (const k of Object.keys(updates)) {
    if (k !== 'params') Vue.set(clip, k, updates[k]);
  }
  store.isDirty = true;
},

deleteCameraClip(clipId) {
  if (!store.project.cameraTrack) return;
  const idx = store.project.cameraTrack.findIndex(c => c.id === clipId);
  if (idx !== -1) {
    store.project.cameraTrack.splice(idx, 1);
    store.isDirty = true;
    actions.commitState();
  }
},
```

#### 7B: Topbar.vue — Kamera toggle

- [ ] **Step 7B-1: Kamera toggle butonu ekle**

`services/web/src/components/toolbar/Topbar.vue` dosyasını aç. Mevcut toolbar buttonlarına benzer şekilde kamera toggle butonu ekle. Toolbarın uygun bir bölümüne (örn. sağ taraf veya view ayarları bölümü):

```html
<button
  :class="['toolbar-btn', { active: project.cameraType === 'moving' }]"
  title="Toggle Moving Camera"
  @click="toggleCamera"
>
  🎥
</button>
```

Script bölümüne:

```js
import { store, actions } from '../../store/project.js';

// computed içine:
project() { return store.project; },

// methods içine:
toggleCamera() {
  const next = store.project.cameraType === 'moving' ? 'static' : 'moving';
  actions.setCameraType(next);
},
```

#### 7C: Timeline.vue — Kamera parçası

- [ ] **Step 7C-1: Timeline'a kamera parçası ekle**

`services/web/src/components/timeline/Timeline.vue` dosyasını aç. Mevcut track listesinin üstünde veya altında, `cameraType === 'moving'` olduğunda kamera parçasını göster.

Template'e ekle (mevcut `v-for="track in visibleTracks"` döngüsünün üstüne):

```html
<!-- Camera Track -->
<div v-if="project.cameraType === 'moving'" class="timeline-track camera-track">
  <div class="track-label text-xs text-violet-300 flex items-center gap-1">
    🎥 Camera
    <button class="ml-auto text-[9px] text-studio-accent hover:opacity-80" @click="addCameraClip">+</button>
  </div>
  <div class="track-clips relative h-8">
    <div
      v-for="clip in cameraClips"
      :key="clip.id"
      class="absolute top-1 h-6 rounded text-[9px] flex items-center px-1 bg-violet-500/30 border border-violet-500/50 text-violet-200 cursor-pointer select-none"
      :style="{ left: timeToPixel(clip.startTime) + 'px', width: timeToPixel(clip.duration) + 'px' }"
      @click="selectCameraClip(clip.id)"
    >
      zoom {{ clip.params?.zoom?.toFixed(1) }}
    </div>
  </div>
</div>
```

Script bölümüne (computed ve methods):

```js
// computed
project() { return store.project; },
cameraClips() { return store.project.cameraTrack || []; },

// methods
addCameraClip() {
  actions.addCameraMoveClip({ startTime: store.playbackTime, duration: 2 });
},
selectCameraClip(clipId) {
  store.selectedClipId = clipId;
},
```

**Not:** `timeToPixel` metodu mevcut Timeline.vue'da zaten vardır; aynı mantığı kullan.

#### 7D: playback.js — camera_move klip önizlemesi

- [ ] **Step 7D-1: computeFrame'e cameraTrack desteği ekle**

`services/web/src/engine/playback.js` dosyasında `computeFrame` metoduna, frame döndürmeden önce kamera animasyonunu hesapla:

```js
computeFrame(time, tracks, objects, project) {
  if (!tracks || !objects) {
    return { objectOverrides: {}, morphShapes: [], hiddenIds: new Set(), cameraState: null };
  }

  const objectMap = this._objectMap || new Map(objects.map(o => [o.id, o]));
  const evaluatedClips = [];

  for (let trackIdx = 0; trackIdx < tracks.length; trackIdx++) {
    const track = tracks[trackIdx];
    if (!track.clips) continue;
    for (const clip of track.clips) {
      const result = this._evaluateClip(clip, time, objectMap);
      if (result) evaluatedClips.push({ trackIndex: trackIdx, clipResult: result });
    }
  }

  const frame = blendClipResults(evaluatedClips, objectMap);
  this._applyEnterExitAnims(frame, time, objects);

  // Camera state
  frame.cameraState = null;
  if (project && project.cameraType === 'moving' && project.cameraTrack?.length > 0) {
    for (const camClip of project.cameraTrack) {
      if (!isClipActive(camClip, time)) continue;
      const progress = getClipProgress(camClip, time);
      const easedT = evaluateEasing(progress, camClip.easing || 'ease_in_out', 0, 1);
      frame.cameraState = {
        x: lerp(0, camClip.params?.targetX || 0, easedT),
        y: lerp(0, camClip.params?.targetY || 0, easedT),
        zoom: lerp(1, camClip.params?.zoom || 1, easedT),
      };
    }
  }

  return frame;
}
```

**Not:** Bu değişiklik `computeFrame`'in imzasına `project` parametresi ekler. Bu fonksiyon tüm çağrı noktalarında güncellenmeli. `seekTo` ve `_tick` metodlarında `this._project`'i sakla:

`play` metodunda:
```js
play(tracks, objects, duration, project) {
  // ... mevcut kod
  this._project = project;
  // ...
}
```

`seekTo` metodunda:
```js
seekTo(time, tracks, objects, project) {
  // ...
  if (project) this._project = project;
  const frame = this.computeFrame(this.currentTime, this._tracks, this._objects, this._project);
  // ...
}
```

`_tick`'te:
```js
const frame = this.computeFrame(this.currentTime, this._tracks, this._objects, this._project);
```

Calling code'u (App.vue veya StageCanvas.vue'da) da bu değişikliğe göre güncellenmeli — `engine.play(tracks, objects, duration, store.project)` şeklinde.

#### 7E: codegen.js — MovingCameraScene

- [ ] **Step 7E-1: generatePythonCode'a cameraType desteği ekle**

`services/api/src/compiler/codegen.js` dosyasında `generatePythonCode` fonksiyonunda, `class MainScene(Scene):` satırını güncelle:

```js
// Önceki
L.push('class MainScene(Scene):');
L.push('    def construct(self):');

// Sonraki
const sceneBase = project.cameraType === 'moving' ? 'MovingCameraScene' : 'Scene';
L.push(`class MainScene(${sceneBase}):`);
L.push('    def construct(self):');
```

Ayrıca clip animations bölümünün sonunda (exit animations'dan önce), camera clips'i işle:

```js
// Camera clips (after regular clips, before exits)
if (project.cameraType === 'moving' && project.cameraTrack?.length > 0) {
  for (const camClip of project.cameraTrack) {
    const dur = camClip.duration;
    const rtStr = rtOpt(dur);
    const rfStr = rfOpt(camClip.easing);
    const mp = stageToManim(
      camClip.params?.targetX || 0,
      camClip.params?.targetY || 0,
      sw, sh
    );
    const zoom = (camClip.params?.zoom || 1).toFixed(2);
    const code = `self.play(self.camera.frame.animate.move_to([${mp.x.toFixed(2)}, ${mp.y.toFixed(2)}, 0]).scale(${zoom})${rtStr}${rfStr})`;
    steps.push({ time: camClip.startTime, order: 1, code, dur });
  }
}
```

#### 7F: Test

- [ ] **Step 7F-1: Test dosyası oluştur**

`services/web/tests/components/camera.test.js`:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { store, actions } from '../../src/store/project.js';

beforeEach(() => {
  actions.newProject('Test', 'visual');
  actions.commitState();
});

describe('setCameraType', () => {
  it('defaults to static', () => {
    expect(store.project.cameraType).toBe('static');
  });

  it('switches to moving', () => {
    actions.setCameraType('moving');
    expect(store.project.cameraType).toBe('moving');
  });

  it('switches back to static', () => {
    actions.setCameraType('moving');
    actions.setCameraType('static');
    expect(store.project.cameraType).toBe('static');
  });
});

describe('addCameraMoveClip', () => {
  it('adds a camera_move clip to cameraTrack', () => {
    actions.setCameraType('moving');
    const clip = actions.addCameraMoveClip({ startTime: 1, duration: 2, targetX: 100, targetY: 50, zoom: 1.5 });
    expect(clip.type).toBe('camera_move');
    expect(store.project.cameraTrack).toHaveLength(1);
    expect(store.project.cameraTrack[0].params.zoom).toBe(1.5);
  });

  it('deleteCameraClip removes from cameraTrack', () => {
    actions.setCameraType('moving');
    const clip = actions.addCameraMoveClip({ startTime: 0, duration: 1 });
    actions.deleteCameraClip(clip.id);
    expect(store.project.cameraTrack).toHaveLength(0);
  });
});
```

- [ ] **Step 7F-2: Testi çalıştır**

```bash
cd services/web && npm run test:unit
```

Expected: All tests pass.

- [ ] **Step 7F-3: Commit**

```bash
git add services/web/src/store/project.js \
        services/web/src/components/toolbar/Topbar.vue \
        services/web/src/components/timeline/Timeline.vue \
        services/web/src/engine/playback.js \
        services/api/src/compiler/codegen.js \
        services/web/tests/components/camera.test.js
git commit -m "feat: camera animations with MovingCameraScene support (Phase 2 feature 4)"
```

---

## Final: Merge ve Doğrulama

- [ ] **Dalga 1 branch'lerini (debt-1, debt-2, debt-3) main'e merge et**
- [ ] **Dalga 2 branch'lerini main'e merge et (çakışmaları çözerek)**
- [ ] **Final testleri çalıştır:**

```bash
cd services/web && npm run test:unit && npm test
```

Expected: All 9+ unit tests pass, all 89 engine tests pass.

- [ ] **Docker build doğrulaması:**

```bash
docker-compose up --build
```

Browser'da `http://localhost:8080`'i aç. Kontrol et:
1. Render butonu nesneler varken aktif
2. Axes nesnesine grafik eklenebildiğinde canvas'ta görünür
3. Paralel klip işaretlenince timeline'da `∥` badge görünür
4. Path draw aracı ile çizilen path `path_move` klibi oluşturuyor
5. 🎥 kamera butonu timeline'da kamera parçasını açıyor
